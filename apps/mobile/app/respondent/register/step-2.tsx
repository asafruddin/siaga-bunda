import { useState } from 'react'; import { Alert, Platform, Pressable, Text } from 'react-native'; import DateTimePicker from '@react-native-community/datetimepicker'; import { router } from 'expo-router'; import { calculateHpl,calculatePregnancyWeeks } from '@siaga/shared'; import { Button, Field, Screen, Title, ui } from '@/components/ui'; import { useRegistration } from '@/features/registration/store';
export default function Step2(){const d=useRegistration();const[hpht,setHpht]=useState(d.hpht??'');const[show,setShow]=useState(false);const[children,setChildren]=useState(String(d.numberOfChildren??0));const[medical,setMedical]=useState(d.medicalHistory??'');const[birth,setBirth]=useState(d.birthHistory??'');const hpl=hpht?calculateHpl(hpht):'';const weeks=hpht?calculatePregnancyWeeks(hpht):0;function next(){if(!hpht||weeks>45)return Alert.alert('HPHT tidak valid','Pilih tanggal hari pertama haid terakhir yang sesuai.');d.set({hpht,hpl,pregnancyAgeWeeks:weeks,numberOfChildren:Number(children),medicalHistory:medical,birthHistory:birth});router.push('/respondent/register/step-3' as never)}return <Screen><Title subtitle="Langkah 2 dari 3">Data Kehamilan</Title><Text style={ui.label}>Hari pertama haid terakhir (HPHT)</Text><Pressable style={ui.input} onPress={()=>setShow(true)}><Text>{hpht||'Pilih tanggal'}</Text></Pressable>{show&&<DateTimePicker value={hpht?new Date(`${hpht}T00:00:00`):new Date()} maximumDate={new Date()} mode="date" onChange={(_,date)=>{if(Platform.OS!=='ios')setShow(false);if(date)setHpht(date.toISOString().slice(0,10))}}/>}<Field label="Hari perkiraan lahir (otomatis)" value={hpl} editable={false}/><Field label="Usia kehamilan (minggu)" value={String(weeks)} editable={false}/><Field label="Jumlah anak" value={children} onChangeText={setChildren} keyboardType="number-pad"/><Field label="Riwayat kesehatan (tulis ‘Tidak ada’ bila tidak ada)" value={medical} onChangeText={setMedical} multiline/><Field label="Riwayat persalinan" value={birth} onChangeText={setBirth} multiline/><Button onPress={next}>Lanjutkan</Button></Screen>}
